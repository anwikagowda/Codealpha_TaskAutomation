# Task Automation with Python

## Description
This is a simple Task Automation project developed using Python.

## Features
- Moves all `.jpg` files from the `images` folder to the `Moved_Images` folder.
- Automatically creates the destination folder if it does not exist.
- Displays a success message after moving each file.

## Technologies Used
- Python
- os
- shutil